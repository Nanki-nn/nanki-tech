# 文章详情页纯标题头部设计

## 目标

进一步精简所有文章详情页头部，只保留返回入口和文章标题。

## 设计

- 不再在文章详情页头部渲染 `post.subtitle` 和 `post.date`。
- 标题、头部分隔线、返回 Blog 链接和正文目录保持不变。
- 副标题与日期数据继续保留，博客列表和其他数据消费者不受影响。
- 只修改共享 `BlogPost` 组件，不调整内容配置。

## 验证

1. 所有文章详情页头部不显示分类和日期。
2. 标题与返回入口之间的间距自然，无空白占位。
3. 桌面与移动宽度下无重叠或横向溢出。
4. 类型检查不新增错误。
